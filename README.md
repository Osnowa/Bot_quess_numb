# Guess Number Telegram Bot

Простой Telegram-бот с игрой «Угадай число», написанный на Python с использованием aiogram

---

## 🚀 Возможности

* Игра «Угадай число» (от 1 до 100)
* Ограниченное количество попыток
* Поддержка нескольких пользователей одновременно
* Подсчёт статистики (сыгранные игры и победы)
* Асинхронная обработка сообщений
* Логирование событий

---

## 🧱 Структура проекта

```
modular_quess_numb/
├── main.py                 # Точка входа
├── config/
│   ├── config.py           # Загрузка конфигурации
│   └── logging_setup.py    # Настройка логирования
├── handlers/
│   ├── user.py             # Основная логика игры
│   └── other.py            # Обработка прочих сообщений
├── lexicon/
│   └── lexicon.py          # Тексты сообщений
├── users.py                # Хранилище пользователей
├── .env                    # Переменные окружения
└── .gitignore
```

---

## ⚙️ Стек технологий

* Python 3.10+
* aiogram 3.x
* asyncio
* logging

---

## ▶️ Как запустить проект

1. Клонировать репозиторий
2. Создать виртуальное окружение и активировать его
3. Установить зависимости
4. Создать файл `.env` и добавить в него:

```env
BOT_TOKEN=your_telegram_bot_token
```

5. Запустить бота:

```bash
python main.py
```

---

## 🧠 Архитектура

* **handlers** — отвечают только за обработку Telegram-апдейтов
* **users.py** — хранит состояние каждого пользователя
* **config** — конфигурация и логирование
* **lexicon** — все текстовые сообщения вынесены отдельно

Проект построен модульно, что упрощает поддержку и расширение функционала.

---

## 📌 Возможные улучшения

* Добавить клавиатуры (ReplyKeyboardMarkup)
* Добавить unit-тесты для логики игры
* Вынести параметры игры (диапазон, попытки) в конфиг

---

## 👨‍💻 Автор

Савин Илья Александрович
